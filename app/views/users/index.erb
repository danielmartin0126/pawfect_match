<h1> All Pets </h1>
<h3>Search</h3>
    <%=form_tag(users_path, method: :post, url: search_users_path) do %>
    <%=text_field_tag(:search, params[:search])%>
    <%=submit_tag "Search"%>
    <% end%>
    <p></p>

<%@users.each do |user| %>
<%= link_to user.name, user_path(user) %>
<% end %>
